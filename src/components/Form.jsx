

const Form = ({labelText, type, name, placeholder, value, handleChange, isGeneratingImage, randomPromptHandler  }) => {
  return (
    <div>
      <div className="flex items-center gap-2 mb-2">

      </div>
    </div>
  )
}

export default Form
